# Mutation kill-rate 证据 — 2026-08-29（wop-dotnet-sdk 首装）

- 命令：`python3 .factory/mutations/run.py`（全量，无 --only 过滤）
- 退出码：**0**（全部按预期；无 FAIL / SKIP / 配置错误；还原零残留——runner 内置逐文件字节校验通过）
- 缺陷集：13 条 = 篡改类 6（guard 门）+ 行为破坏类 5（tests 门）+ 负例 2（guard/tests 各 1）
- 缺陷集为本仓本地化（defects.json 锚点全部指向 wop-dotnet-sdk 真实文件），
  非上游/gateway 原版锚点。

## 汇总（原文摘录）

```
===== mutation 冒烟汇总 =====
  [G-01] PASS       blocked=True（rc=1）符合预期
  [G-02] PASS       blocked=True（rc=1）符合预期
  [G-03] PASS       blocked=True（rc=2）符合预期
  [G-04] PASS       blocked=True（rc=1）符合预期
  [M-01] PASS       blocked=True（rc=2）符合预期
  [M-02] PASS       blocked=True（rc=2）符合预期
  [B-01] PASS       blocked=False（rc=0）符合预期
  [B-101] PASS      blocked=True（rc=1）符合预期
  [B-102] PASS      blocked=True（rc=1）符合预期
  [B-103] PASS      blocked=True（rc=1）符合预期
  [B-104] PASS      blocked=True（rc=1）符合预期
  [B-105] PASS      blocked=True（rc=1）符合预期
  [B-106] PASS      blocked=False（rc=0）符合预期
  篡改类拦截（guard 门）: 6/6 = 100%
  行为破坏类拦截（tests 门）: 5/5 = 100%
  正向缺陷拦截（kill rate）: 11/11 = 100%
  负例放行: 2/2
  结论: 门灵敏度冒烟通过（auto-merge 的必要非充分条件）
  周界指纹已绑定: c1e87d2ff554（evidence-stamp.json）
```

## 行为破坏类逐条击杀证据（注入 → 变红的测试，注入后实测末行）

| id | target | 注入 | kill 证据（注入后 dotnet test 实测变红） |
|---|---|---|---|
| B-101 | src/Wop.Sdk/Transport.cs | 上限检查短路 `if (body.Length > MaxResponseBytes)` → `if (false && …)` | `ErrorContractTests.契约_响应体恰超上限_明确文案` [FAIL]（TransportTests.Send_响应体超上限_拒绝 同套件红） |
| B-102 | src/Wop.Sdk/WopException.cs | Fuzzy 文案分支反置 | `WopClientTests.VerifyResponse_tamper密文_解密失败模糊_I7` [FAIL]（WopExceptionTests/InvariantsTests I7 用例红） |
| B-103 | src/Wop.Sdk/RequestDraft.cs | `VerifyResult.Fail` 错误码丢失 → null | `WopClientTests.VerifyResponse_dekAlg跨族_AlgMismatch_I3` [FAIL]（ErrorCode 断言族红） |
| B-104 | src/Wop.Sdk/WopClient.cs | BuildRequest digest 门短路（D2） | `商户发起 L0 签名请求 digest 必产且入签` [FAIL]（Reqnroll 旅程用例红） |
| B-105 | src/Wop.Sdk/WopHeaders.cs | AppKey 头常量改名 | `TransportTests.Send_自定义Handler无网络_请求头与体透传` [FAIL] |
| B-106（负例） | src/Wop.Sdk/WopException.cs | XML doc 措辞变更（行为等价） | 388 用例仍绿，门放行 rc=0（防门全拒） |

每条 find 均经预验证「恰好命中一次 + 注入后 dotnet test 真红 + 字节还原一致」后才入缺陷集。

## 口径说明

- tests 门命令：`scripts/run_tests.sh --no-lock`（= `dotnet build Wop.Sdk.sln` +
  `dotnet test Wop.Sdk.sln --no-build`，388 tests 全量；实测门基线 ~6s）。
- 退出码语义：rc=1 = 击杀证据；rc=0 = 放行；域外退出码/超时 = 无效运行
  （judge 判 FAIL，既不奖励击杀也不奖励放行）。guard 门 rc=2 为 fail-closed
  设计（=拦截，G-03/M-01/M-02 实测）。
- 门超时预算：guard 300s / tests 600s（tests 门实测基线 ~6s，≈100 倍余量）。
- 行为类 kill rate 100%（≥80% 为下限；低于 100% 即缺陷选材或门有洞）。
- 周界指纹绑定：evidence-stamp.json `perimeter_blob=c1e87d2ff554…` 对应本次
  提交的 factory-local.json git blob；改周界配置后必须重跑本 runner 重证。
  （stamp 内 `evidence` 字段为 run.py 分发版默认文件名 "EVIDENCE-2026-08-24.md"，
  本仓证据实际留档于本文件 EVIDENCE-2026-08-29.md；该字段无下游消费方。）
- 本证据为「必要非充分」条件；auto-merge 的开启属治理动作，由人类决定
  （本仓 auto-merge 默认关闭）。
